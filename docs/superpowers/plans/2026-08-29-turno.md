# Plan de implementación — Turno

> **Para agentes:** SUB-SKILL REQUERIDA: usar `superpowers:subagent-driven-development`
> (recomendado) o `superpowers:executing-plans` para implementar tarea por tarea.
> Los pasos usan checkbox (`- [ ]`) para el seguimiento.

**Objetivo:** que un paciente logueado pueda tomar un hueco y cancelarlo, y que
ese hueco desaparezca y reaparezca en consecuencia.

**Arquitectura:** una entidad nueva (`Turno`) con su repositorio, y un servicio
que **no reimplementa ninguna regla del calendario**: para reservar, le pide los
huecos al servicio de agenda y verifica que el `inicio` pedido esté entre ellos.
Eso arrastra gratis horario, bloqueos, anticipación, horizonte y estado del
profesional.

**Stack:** Go 1.25, stdlib, `github.com/google/uuid`, `golang.org/x/crypto`.
**Sin dependencias nuevas.**

**Spec:** [2026-08-29-turno-design.md](../specs/2026-08-29-turno-design.md)
**Base:** rama `refactor-gian`, commit `5f4d09d`

---

## Restricciones globales

Aplican a **todas** las tareas. No se repiten en cada una.

- **Idioma:** todo en español —tipos, funciones, campos, constantes,
  comentarios, mensajes y claves JSON—. Paquetes en inglés. `String()` y
  `Error()` en inglés por las interfaces de Go. Las claves
  `type`/`title`/`status`/`detail` de RFC 7807 en inglés.
- **Sin acentos ni eñes en identificadores ni en claves JSON.** Los mensajes de
  error sí llevan acentos.
- **Sin mocks.** Los repositorios en memoria son el doble de test.
- **`internal/domain` no importa nada del proyecto.** El CI lo verifica.
- **Intervalos semiabiertos en todo el sistema.** `[inicio, fin)`. Un turno que
  termina 09:50 no se solapa con uno que empieza 09:50. Usar `<=` en cualquiera
  de las dos comparaciones hace desaparecer un turno por día sin que nadie
  entienda por qué. Ver `Bloqueo.SeSolapaCon` en `internal/domain/bloqueo.go:87`.
- **Antes de cada commit:** `make check` (fmt + test-race + lint). Si `make` no
  está disponible: `go fmt ./... && go vet ./... && go test ./... -race &&
  golangci-lint run ./...`.
- **`-race` en Windows** necesita `CGO_ENABLED=1` y gcc en el PATH. Ver
  `apps/api/README.md`.
- **Módulo:** `github.com/joaquinfochoa/Salud/apps/api`. Todos los comandos se
  corren desde `apps/api/`.

---

## Pre-flight: dónde rompe esta etapa

Se verificó **antes** de partir las tareas, porque en la etapa de autenticación
no se hizo y el plan quedó mal partido.

| Cambio | ¿Rompe? |
|---|---|
| `Turno`, `EstadoTurno`, repositorio, servicio y handler de turnos | **No.** Todo código nuevo. |
| `CalculoHuecos` gana el campo `Turnos []Turno` | **No.** Los 16 literales existentes usan nombres de campo, así que el campo nuevo queda en su cero: un slice `nil`, que significa "no hay turnos que restar". Verificado con `grep -A1 'CalculoHuecos{'`: ninguno es posicional. |
| `service.NuevaAgenda` gana el repositorio de turnos | **Sí.** 6 call sites. |
| `ReemplazarHorarios` y `CrearBloqueo` devuelven cuántos turnos cancelaron | **Sí.** 14 call sites en tests. |

**Las dos rupturas viven en el mismo archivo y en la misma idea** —"la agenda
ahora conoce los turnos"— así que van juntas en la **Task 7**, la última antes
de la verificación. Todo lo anterior entra en commits limpios.

**No hay ciclo entre servicios**, y conviene decirlo porque es el error natural:
`service.Turno` depende de `*service.Agenda` (para pedirle los huecos), y
`service.Agenda` depende de `repository.Turno` (para restarlos y cancelarlos),
**no** de `service.Turno`. La regla de cancelar vive en el dominio, que los dos
importan.

---

## Estructura de archivos

**Crear:**

| Archivo | Responsabilidad |
|---|---|
| `internal/domain/turno.go` | Entidad `Turno`, enum `EstadoTurno`, transición de cancelación |
| `internal/repository/turno.go` | Interfaz `repository.Turno` |
| `internal/repository/memory/turno.go` | Implementación en memoria |
| `internal/service/turno.go` | Reservar, cancelar y los dos listados |
| `internal/handler/turno.go` | Los 4 endpoints |
| `internal/handler/dto_turno.go` | DTOs de entrada y salida |

**Modificar:**

| Archivo | Qué cambia |
|---|---|
| `internal/domain/errores.go` | 3 centinelas nuevos |
| `internal/domain/huecos.go` | `CalculoHuecos.Turnos` y su resta |
| `internal/service/agenda.go` | Repositorio de turnos; resta en huecos; cancelación en bloqueo y horarios |
| `internal/handler/agenda.go` | Las dos respuestas informan turnos cancelados |
| `internal/handler/dto_agenda.go` | Idem |
| `internal/handler/router.go` | 4 rutas nuevas |
| `internal/handler/problema.go` | Mapeo de los centinelas nuevos |
| `api/openapi.yaml` | 4 operaciones nuevas y 2 respuestas que cambian |
| `cmd/api/main.go` | Cableado |
| `apps/api/README.md` | La sección de arquitectura |

---

## Task 1: entidad `Turno`

**Archivos:**
- Crear: `internal/domain/turno.go`, `internal/domain/turno_test.go`
- Modificar: `internal/domain/errores.go`

**Interfaces:**
- Consume: `Hueco`, `Modalidad`, `ErrorValidacion` del mismo paquete
- Produce:
  - `domain.EstadoTurno` con `TurnoReservado` y `TurnoCancelado`, y `EsValido() bool`
  - `domain.Turno{ID, ProfesionalID, PacienteID uuid.UUID; Inicio, Fin time.Time;
    Modalidad Modalidad; Estado EstadoTurno; Motivo string; CreadoEn time.Time;
    CanceladoEn *time.Time; CanceladoPor *uuid.UUID}`
  - `domain.NuevoTurno(profesionalID, pacienteID uuid.UUID, hueco Hueco, motivo string, ahora time.Time) (Turno, error)`
  - `Turno.Cancelar(porUsuarioID uuid.UUID, ahora time.Time) (Turno, error)`
  - `Turno.EstaActivo() bool`, `Turno.SeSolapaCon(inicio, fin time.Time) bool`,
    `Turno.Clonar() Turno`
  - Centinelas `ErrHuecoTomado`, `ErrPacienteSolapado`, `ErrTurnoAjeno`

- [ ] **Paso 1: escribir el test que falla**

`internal/domain/turno_test.go`:

```go
package domain

import (
	"errors"
	"strings"
	"testing"
	"time"

	"github.com/google/uuid"
)

func huecoDePrueba() Hueco {
	inicio := time.Date(2026, 9, 15, 10, 0, 0, 0, ZonaHoraria)
	return Hueco{Inicio: inicio, Fin: inicio.Add(50 * time.Minute), Modalidad: ModalidadTelemedicina}
}

func TestNuevoTurno(t *testing.T) {
	profesionalID, pacienteID := uuid.New(), uuid.New()
	hueco := huecoDePrueba()
	ahora := time.Date(2026, 9, 1, 12, 0, 0, 0, time.UTC)

	turno, err := NuevoTurno(profesionalID, pacienteID, hueco, "  dolor lumbar  ", ahora)
	if err != nil {
		t.Fatalf("NuevoTurno devolvió error: %v", err)
	}

	if turno.ProfesionalID != profesionalID || turno.PacienteID != pacienteID {
		t.Error("las dos partes del turno no quedaron guardadas")
	}
	if !turno.Inicio.Equal(hueco.Inicio) || !turno.Fin.Equal(hueco.Fin) {
		t.Errorf("intervalo = [%v, %v), se esperaba [%v, %v)", turno.Inicio, turno.Fin, hueco.Inicio, hueco.Fin)
	}
	// La modalidad se copia del hueco. Si el profesional después cambia el
	// bloque de presencial a telemedicina, este turno tiene que seguir
	// diciendo lo que se pactó.
	if turno.Modalidad != hueco.Modalidad {
		t.Errorf("Modalidad = %q, se esperaba %q", turno.Modalidad, hueco.Modalidad)
	}
	if turno.Estado != TurnoReservado {
		t.Errorf("Estado = %q, se esperaba reservado", turno.Estado)
	}
	if turno.Motivo != "dolor lumbar" {
		t.Errorf("Motivo = %q, se esperaba recortado", turno.Motivo)
	}
	if !turno.CreadoEn.Equal(ahora) {
		t.Errorf("CreadoEn = %v", turno.CreadoEn)
	}
	if turno.CanceladoEn != nil || turno.CanceladoPor != nil {
		t.Error("un turno nuevo no puede nacer cancelado")
	}
}

func TestNuevoTurnoSinMotivo(t *testing.T) {
	// El motivo es opcional: nadie tiene por qué justificar una consulta para
	// poder pedirla.
	if _, err := NuevoTurno(uuid.New(), uuid.New(), huecoDePrueba(), "", time.Now()); err != nil {
		t.Errorf("un turno sin motivo debería ser válido: %v", err)
	}
}

func TestNuevoTurnoMotivoDemasiadoLargo(t *testing.T) {
	_, err := NuevoTurno(uuid.New(), uuid.New(), huecoDePrueba(), strings.Repeat("a", 501), time.Now())

	var verr ErrorValidacion
	if !errors.As(err, &verr) {
		t.Fatalf("se esperaba ErrorValidacion, se obtuvo %v", err)
	}
	if verr.Campos[0].Campo != "motivo" {
		t.Errorf("campo = %q, se esperaba motivo", verr.Campos[0].Campo)
	}
}

func TestCancelar(t *testing.T) {
	ahora := time.Date(2026, 9, 1, 12, 0, 0, 0, time.UTC)
	quienCancela := uuid.New()

	turno, err := NuevoTurno(uuid.New(), uuid.New(), huecoDePrueba(), "", ahora)
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}

	cancelado, err := turno.Cancelar(quienCancela, ahora)
	if err != nil {
		t.Fatalf("Cancelar devolvió error: %v", err)
	}

	if cancelado.Estado != TurnoCancelado {
		t.Errorf("Estado = %q, se esperaba cancelado", cancelado.Estado)
	}
	if cancelado.CanceladoEn == nil || !cancelado.CanceladoEn.Equal(ahora) {
		t.Error("CanceladoEn no quedó registrado")
	}
	if cancelado.CanceladoPor == nil || *cancelado.CanceladoPor != quienCancela {
		t.Error("CanceladoPor no quedó registrado")
	}
	// Receptor por valor: el original no se toca.
	if turno.Estado != TurnoReservado {
		t.Error("Cancelar mutó el receptor")
	}
}

func TestCancelarDosVecesFalla(t *testing.T) {
	ahora := time.Date(2026, 9, 1, 12, 0, 0, 0, time.UTC)
	turno, err := NuevoTurno(uuid.New(), uuid.New(), huecoDePrueba(), "", ahora)
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}

	cancelado, err := turno.Cancelar(uuid.New(), ahora)
	if err != nil {
		t.Fatalf("primer Cancelar: %v", err)
	}

	// No es idempotente a propósito: repetir la operación probablemente
	// significa que el cliente cree algo distinto de lo que pasó.
	if _, err := cancelado.Cancelar(uuid.New(), ahora); err == nil {
		t.Error("cancelar dos veces debería fallar")
	}
}

// El borde: un turno se puede cancelar hasta el instante en que empieza, no
// después. Los dos casos, porque un > mal escrito pasa desapercibido con uno
// solo.
func TestCancelarEnElBorde(t *testing.T) {
	hueco := huecoDePrueba()
	turno, err := NuevoTurno(uuid.New(), uuid.New(), hueco, "", hueco.Inicio.Add(-time.Hour))
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}

	if _, err := turno.Cancelar(uuid.New(), hueco.Inicio.Add(-time.Nanosecond)); err != nil {
		t.Errorf("un instante antes de empezar todavía se cancela: %v", err)
	}
	if _, err := turno.Cancelar(uuid.New(), hueco.Inicio); err == nil {
		t.Error("en el instante exacto de inicio ya no se cancela")
	}
	if _, err := turno.Cancelar(uuid.New(), hueco.Fin.Add(time.Hour)); err == nil {
		t.Error("un turno pasado no se cancela")
	}
}

func TestTurnoEstaActivo(t *testing.T) {
	ahora := time.Date(2026, 9, 1, 12, 0, 0, 0, time.UTC)
	turno, err := NuevoTurno(uuid.New(), uuid.New(), huecoDePrueba(), "", ahora)
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}
	if !turno.EstaActivo() {
		t.Error("un turno recién reservado está activo")
	}

	cancelado, err := turno.Cancelar(uuid.New(), ahora)
	if err != nil {
		t.Fatalf("Cancelar: %v", err)
	}
	if cancelado.EstaActivo() {
		t.Error("un turno cancelado no está activo")
	}
}

// Semiabierto, igual que Bloqueo.SeSolapaCon: un turno que termina 10:50 no
// pisa uno que empieza 10:50.
func TestTurnoSeSolapaCon(t *testing.T) {
	hueco := huecoDePrueba() // 10:00 a 10:50
	turno, err := NuevoTurno(uuid.New(), uuid.New(), hueco, "", time.Now())
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}

	casos := []struct {
		nombre         string
		desplazamiento time.Duration
		duracion       time.Duration
		esperado       bool
	}{
		{"identico", 0, 50 * time.Minute, true},
		{"empieza adentro", 25 * time.Minute, 50 * time.Minute, true},
		{"termina adentro", -25 * time.Minute, 50 * time.Minute, true},
		{"lo contiene", -time.Hour, 3 * time.Hour, true},
		{"pegado despues", 50 * time.Minute, 50 * time.Minute, false},
		{"pegado antes", -50 * time.Minute, 50 * time.Minute, false},
		{"muy despues", 5 * time.Hour, 50 * time.Minute, false},
	}

	for _, c := range casos {
		t.Run(c.nombre, func(t *testing.T) {
			inicio := hueco.Inicio.Add(c.desplazamiento)
			if got := turno.SeSolapaCon(inicio, inicio.Add(c.duracion)); got != c.esperado {
				t.Errorf("SeSolapaCon = %v, se esperaba %v", got, c.esperado)
			}
		})
	}
}

func TestTurnoClonar(t *testing.T) {
	ahora := time.Date(2026, 9, 1, 12, 0, 0, 0, time.UTC)
	turno, err := NuevoTurno(uuid.New(), uuid.New(), huecoDePrueba(), "", ahora)
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}
	cancelado, err := turno.Cancelar(uuid.New(), ahora)
	if err != nil {
		t.Fatalf("Cancelar: %v", err)
	}

	clon := cancelado.Clonar()
	*clon.CanceladoEn = clon.CanceladoEn.Add(time.Hour)
	*clon.CanceladoPor = uuid.New()

	if cancelado.CanceladoEn.Equal(*clon.CanceladoEn) {
		t.Error("mutar CanceladoEn del clon alteró el original")
	}
	if *cancelado.CanceladoPor == *clon.CanceladoPor {
		t.Error("mutar CanceladoPor del clon alteró el original")
	}
}

func TestEstadoTurnoEsValido(t *testing.T) {
	for _, e := range []EstadoTurno{TurnoReservado, TurnoCancelado} {
		if !e.EsValido() {
			t.Errorf("%q debería ser válido", e)
		}
	}
	for _, e := range []EstadoTurno{"", "atendido", "RESERVADO"} {
		if e.EsValido() {
			t.Errorf("%q no debería ser válido", e)
		}
	}
}
```

- [ ] **Paso 2: correr el test y verificar que falla**

```bash
go test ./internal/domain/ -run TestNuevoTurno -v
```

Esperado: FAIL — `undefined: NuevoTurno`.

- [ ] **Paso 3: escribir la implementación**

`internal/domain/turno.go`:

```go
package domain

import (
	"fmt"
	"strings"
	"time"
	"unicode/utf8"

	"github.com/google/uuid"
)

// maxLargoMotivoTurno es más corto que el de Bio a propósito: un motivo de
// consulta es una línea, no una historia clínica. Y no debe serlo: cuanto menos
// dato de salud viaje y se guarde, menos superficie tiene la Ley 25.326.
const maxLargoMotivoTurno = 500

// EstadoTurno tiene dos valores y no cuatro.
//
// "atendido" y "ausente" se difirieron: hoy nada consume esa información —no
// hay cobro, ni reputación, ni política de ausencias— y agregarlas después es
// una constante acá, un método de transición y un endpoint, sin migración.
//
// Un turno pasado no necesita estado: eso lo dice el reloj.
type EstadoTurno string

const (
	TurnoReservado EstadoTurno = "reservado"
	TurnoCancelado EstadoTurno = "cancelado"
)

func (e EstadoTurno) EsValido() bool {
	switch e {
	case TurnoReservado, TurnoCancelado:
		return true
	}
	return false
}

// Turno es un acuerdo entre un paciente y un profesional para un intervalo
// concreto.
//
// No hay entidad Paciente: PacienteID referencia a un Usuario. Una entidad que
// solo tuviera un UsuarioID y nada más es ceremonia; aparece el día que el
// paciente necesite datos que un usuario no tiene.
type Turno struct {
	ID            uuid.UUID
	ProfesionalID uuid.UUID
	PacienteID    uuid.UUID

	// Inicio, Fin y Modalidad son una copia del hueco al momento de reservar,
	// no una lectura del horario actual. Si el profesional convierte los
	// martes de presencial a telemedicina, este turno sigue diciendo lo que se
	// pactó: leerlo del horario lo reescribiría hacia atrás.
	Inicio    time.Time
	Fin       time.Time
	Modalidad Modalidad

	Estado   EstadoTurno
	Motivo   string
	CreadoEn time.Time

	// CanceladoEn y CanceladoPor existen desde el día uno aunque nada los lea
	// todavía. El día que haya una política de ausencias —"cancelar con menos
	// de 24 horas cuenta como ausencia"— este dato no se puede reconstruir si
	// no se guardó. Mismo argumento que DadoDeBajaEn en Profesional.
	//
	// CanceladoPor es un UsuarioID y no un enum paciente|profesional: guarda
	// más por el mismo precio, y quién es cada uno se deduce comparando contra
	// PacienteID.
	CanceladoEn  *time.Time
	CanceladoPor *uuid.UUID
}

// NuevoTurno recibe el hueco entero y no un inicio suelto. Es deliberado: Fin y
// Modalidad salen de ahí, así que no hay forma de construir un turno de tres
// horas en una agenda de cincuenta minutos ni de inventarse una modalidad que
// el profesional no ofrece.
func NuevoTurno(profesionalID, pacienteID uuid.UUID, hueco Hueco, motivo string, ahora time.Time) (Turno, error) {
	var verr ErrorValidacion

	limpio := strings.TrimSpace(motivo)
	if utf8.RuneCountInString(limpio) > maxLargoMotivoTurno {
		verr.agregar("motivo", fmt.Sprintf("no puede superar los %d caracteres", maxLargoMotivoTurno))
	}

	if verr.tieneErrores() {
		return Turno{}, verr
	}

	return Turno{
		ID:            uuid.New(),
		ProfesionalID: profesionalID,
		PacienteID:    pacienteID,
		Inicio:        hueco.Inicio,
		Fin:           hueco.Fin,
		Modalidad:     hueco.Modalidad,
		Estado:        TurnoReservado,
		Motivo:        limpio,
		CreadoEn:      ahora,
	}, nil
}

// Cancelar devuelve el turno cancelado sin tocar el receptor.
//
// No es idempotente, a diferencia del logout: cancelar dos veces probablemente
// significa que el cliente cree algo distinto de lo que pasó, y decírselo es
// más útil que devolverle un éxito falso.
//
// Un turno que ya empezó no se cancela. Cancelarlo hacia atrás borraría el
// registro de una consulta que efectivamente ocurrió.
func (t Turno) Cancelar(porUsuarioID uuid.UUID, ahora time.Time) (Turno, error) {
	switch {
	case t.Estado == TurnoCancelado:
		return Turno{}, ErrorValidacion{Campos: []ErrorCampo{
			{Campo: "estado", Mensaje: "el turno ya está cancelado"},
		}}
	case !t.Inicio.After(ahora):
		return Turno{}, ErrorValidacion{Campos: []ErrorCampo{
			{Campo: "inicio", Mensaje: "el turno ya empezó y no se puede cancelar"},
		}}
	}

	t.Estado = TurnoCancelado
	t.CanceladoEn = &ahora
	t.CanceladoPor = &porUsuarioID
	return t, nil
}

// EstaActivo dice si el turno sigue ocupando su hueco. Un turno cancelado lo
// libera, que es el punto de cancelar.
func (t Turno) EstaActivo() bool {
	return t.Estado == TurnoReservado
}

// SeSolapaCon dice si el turno pisa el intervalo [inicio, fin).
//
// Semiabierto, igual que Bloqueo.SeSolapaCon: un turno que termina 10:50 no
// pisa uno que empieza 10:50. Usar <= en cualquiera de las dos comparaciones
// haría desaparecer un turno por día.
func (t Turno) SeSolapaCon(inicio, fin time.Time) bool {
	return inicio.Before(t.Fin) && fin.After(t.Inicio)
}

// Clonar devuelve una copia profunda. Los dos punteros importan: sin esto,
// quien reciba la copia puede correrle la fecha de cancelación al original.
func (t Turno) Clonar() Turno {
	c := t
	if t.CanceladoEn != nil {
		v := *t.CanceladoEn
		c.CanceladoEn = &v
	}
	if t.CanceladoPor != nil {
		v := *t.CanceladoPor
		c.CanceladoPor = &v
	}
	return c
}
```

- [ ] **Paso 4: agregar los centinelas**

En `internal/domain/errores.go`, dentro del bloque `var (...)`:

```go
	// ErrHuecoTomado lo devuelve el repositorio al escribir: dos pacientes
	// pidieron el mismo hueco. El servicio también lo chequea antes, pero la
	// garantía está en la escritura bajo lock, igual que la matrícula.
	ErrHuecoTomado = errors.New("el hueco ya fue reservado")

	// ErrPacienteSolapado lo devuelve el servicio: el paciente ya tiene otro
	// turno activo que se pisa con este, aunque sea con otro profesional.
	ErrPacienteSolapado = errors.New("ya tenes otro turno a esa hora")

	// ErrTurnoAjeno lo devuelve el servicio cuando quien opera no es ni el
	// paciente ni el profesional del turno. Es distinto de ErrNoAutorizado
	// —que habla de ser dueño de un perfil— porque un turno tiene dos dueños.
	ErrTurnoAjeno = errors.New("el turno no es tuyo")
```

- [ ] **Paso 5: correr los tests y verificar que pasan**

```bash
go test ./internal/domain/ -race -v
```

Esperado: PASS. Toda la suite del paquete.

- [ ] **Paso 6: verificar el aislamiento del dominio**

```bash
go list -deps ./internal/domain/ | grep 'joaquinfochoa' | grep -v 'internal/domain'
```

Esperado: sin salida.

- [ ] **Paso 7: commit**

```bash
git add internal/domain/turno.go internal/domain/turno_test.go internal/domain/errores.go
git commit -m "feat(domain): entidad Turno"
```

---

## Task 2: los huecos restan turnos

Tarea corta y **puramente aditiva**: los 16 literales de `CalculoHuecos` usan
nombres de campo, así que el campo nuevo queda en `nil` y nada existente cambia
de comportamiento.

**Archivos:**
- Modificar: `internal/domain/huecos.go`, `internal/domain/huecos_test.go`

**Interfaces:**
- Produce: campo `CalculoHuecos.Turnos []Turno`

- [ ] **Paso 1: escribir el test que falla**

Agregar a `internal/domain/huecos_test.go`:

```go
func TestHuecosRestanTurnosTomados(t *testing.T) {
	// El bloque de lunes 09:00-13:00 con 50 minutos genera 4 huecos.
	lunes := time.Date(2026, 9, 7, 0, 0, 0, 0, ZonaHoraria)
	segundo := time.Date(2026, 9, 7, 9, 50, 0, 0, ZonaHoraria)

	c := CalculoHuecos{
		Horarios: []HorarioSemanal{bloqueLunes()},
		Turnos: []Turno{{
			Inicio: segundo,
			Fin:    segundo.Add(50 * time.Minute),
			Estado: TurnoReservado,
		}},
		Desde: lunes,
		Hasta: lunes.AddDate(0, 0, 1),
		Ahora: lunes.AddDate(0, 0, -7),
	}

	huecos := c.Generar()
	if len(huecos) != 3 {
		t.Fatalf("se generaron %d huecos, se esperaban 3 (4 menos el reservado)", len(huecos))
	}
	for _, h := range huecos {
		if h.Inicio.Equal(segundo) {
			t.Error("el hueco reservado sigue apareciendo")
		}
	}
}

// Cancelar libera el hueco: es el punto de cancelar.
func TestHuecosNoRestanTurnosCancelados(t *testing.T) {
	lunes := time.Date(2026, 9, 7, 0, 0, 0, 0, ZonaHoraria)
	segundo := time.Date(2026, 9, 7, 9, 50, 0, 0, ZonaHoraria)

	c := CalculoHuecos{
		Horarios: []HorarioSemanal{bloqueLunes()},
		Turnos: []Turno{{
			Inicio: segundo,
			Fin:    segundo.Add(50 * time.Minute),
			Estado: TurnoCancelado,
		}},
		Desde: lunes,
		Hasta: lunes.AddDate(0, 0, 1),
		Ahora: lunes.AddDate(0, 0, -7),
	}

	if huecos := c.Generar(); len(huecos) != 4 {
		t.Errorf("se generaron %d huecos, se esperaban 4: un turno cancelado no ocupa nada", len(huecos))
	}
}

// El mismo borde que los bloqueos: un turno que termina 09:50 no tapa el hueco
// que empieza 09:50.
func TestElBordeDelTurnoNoComeElHuecoSiguiente(t *testing.T) {
	lunes := time.Date(2026, 9, 7, 0, 0, 0, 0, ZonaHoraria)
	primero := time.Date(2026, 9, 7, 9, 0, 0, 0, ZonaHoraria)

	c := CalculoHuecos{
		Horarios: []HorarioSemanal{bloqueLunes()},
		Turnos: []Turno{{
			Inicio: primero,
			Fin:    primero.Add(50 * time.Minute),
			Estado: TurnoReservado,
		}},
		Desde: lunes,
		Hasta: lunes.AddDate(0, 0, 1),
		Ahora: lunes.AddDate(0, 0, -7),
	}

	huecos := c.Generar()
	if len(huecos) != 3 {
		t.Fatalf("se generaron %d huecos, se esperaban 3", len(huecos))
	}
	if !huecos[0].Inicio.Equal(primero.Add(50 * time.Minute)) {
		t.Errorf("el primer hueco vivo es %v, se esperaba 09:50", huecos[0].Inicio)
	}
}
```

> `bloqueLunes()` ya existe como helper en `internal/domain/huecos_test.go`. Si
> el nombre difiere, usar el que esté: es el bloque de lunes 09:00–13:00 con
> duración de 50 minutos que usan los demás tests del archivo.

- [ ] **Paso 2: correr el test y verificar que falla**

```bash
go test ./internal/domain/ -run TestHuecosRestan -v
```

Esperado: FAIL — `unknown field Turnos in struct literal`.

- [ ] **Paso 3: escribir la implementación**

En `internal/domain/huecos.go`, dentro de `CalculoHuecos`, después de
`Bloqueos`:

```go
	// Turnos son los turnos ya tomados. Solo los activos ocupan: cancelar
	// libera el hueco.
	//
	// Es un campo y no un filtro previo del servicio para que la regla de
	// "qué ocupa un hueco" viva entera en un solo lugar, junto a los bloqueos.
	Turnos []Turno
```

Y reemplazar la guarda de `huecosDelBloque` para que consulte las dos cosas:

```go
		case c.ocupado(inicio, fin):
			continue
```

reemplazando `case c.bloqueado(inicio, fin):`. Después, junto a `bloqueado`:

```go
// ocupado dice si algo tapa el intervalo: un bloqueo del profesional o un turno
// ya tomado. Los dos se restan igual; la única diferencia es que un turno
// cancelado no cuenta.
func (c CalculoHuecos) ocupado(inicio, fin time.Time) bool {
	if c.bloqueado(inicio, fin) {
		return true
	}
	for _, turno := range c.Turnos {
		if turno.EstaActivo() && turno.SeSolapaCon(inicio, fin) {
			return true
		}
	}
	return false
}
```

`bloqueado` se conserva tal como está: sigue siendo la mitad de la respuesta y
tiene sus propios tests.

- [ ] **Paso 4: correr los tests y verificar que pasan**

```bash
go test ./internal/domain/ -race -v
```

Esperado: PASS, incluidos los 15 tests de huecos que ya existían. Que esos
sigan pasando sin tocarlos **es** la verificación de que el campo nuevo es
aditivo.

- [ ] **Paso 5: mutation testing del filtro de cancelados**

Cambiar `turno.EstaActivo() &&` por `true &&` en `ocupado`.

```bash
go test ./internal/domain/ -run TestHuecosNoRestanTurnosCancelados
```

Esperado: FAIL. Revertir.

Sin este paso no hay forma de saber si el filtro de cancelados está probado o
simplemente no molesta.

- [ ] **Paso 6: commit**

```bash
git add internal/domain/huecos.go internal/domain/huecos_test.go
git commit -m "feat(domain): los huecos restan los turnos activos"
```

---

## Task 3: repositorio de turnos

**Archivos:**
- Crear: `internal/repository/turno.go`, `internal/repository/memory/turno.go`,
  `internal/repository/memory/turno_test.go`

**Interfaces:**
- Produce: interfaz `repository.Turno` con `Crear`, `ObtenerPorID`,
  `Actualizar`, `ListarDeProfesional(ctx, profesionalID, desde, hasta)` y
  `ListarDePaciente(ctx, pacienteID, desde, hasta)`; y
  `memory.NuevoTurno() *memory.Turno`

- [ ] **Paso 1: escribir el test que falla**

`internal/repository/memory/turno_test.go` cubre, siguiendo el patrón de
`profesional_test.go`:

| Test | Verifica |
|---|---|
| `TestTurnoCrearYObtener` | ida y vuelta por ID |
| `TestTurnoHuecoTomado` | dos turnos activos del mismo profesional que se pisan → `ErrHuecoTomado` |
| `TestTurnoHuecoLiberadoPorCancelacion` | si el primero está cancelado, el segundo entra |
| `TestTurnoPacienteSolapado` | mismo paciente, **distinto** profesional, horarios que se pisan → `ErrPacienteSolapado` |
| `TestTurnoPegadosNoChocan` | uno termina 10:50 y el otro empieza 10:50: los dos entran |
| `TestTurnoListarDeProfesional` | filtra por profesional y por rango `[desde, hasta)` |
| `TestTurnoListarDePaciente` | idem por paciente |
| `TestTurnoListadosIncluyenCancelados` | un turno cancelado sigue apareciendo en los dos listados |
| `TestTurnoDevuelveCopias` | mutar `CanceladoEn` de lo devuelto no toca lo guardado |
| `TestTurnoOrdenTotal` | dos llamadas idénticas devuelven el mismo orden |

El de la unicidad es el que más importa que sea explícito:

```go
func TestTurnoHuecoTomado(t *testing.T) {
	ctx := context.Background()
	r := NuevoTurno()
	profesionalID := uuid.New()
	hueco := huecoDePruebaRepo()

	primero, err := domain.NuevoTurno(profesionalID, uuid.New(), hueco, "", time.Now().UTC())
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}
	if err := r.Crear(ctx, primero); err != nil {
		t.Fatalf("Crear: %v", err)
	}

	// otro paciente, mismo profesional, mismo intervalo
	segundo, err := domain.NuevoTurno(profesionalID, uuid.New(), hueco, "", time.Now().UTC())
	if err != nil {
		t.Fatalf("NuevoTurno: %v", err)
	}

	// Chequear la disponibilidad en una llamada y escribir en otra son dos
	// operaciones, y entre las dos entra el otro paciente. Bajo un mismo lock
	// son una sola.
	if err := r.Crear(ctx, segundo); !errors.Is(err, domain.ErrHuecoTomado) {
		t.Errorf("se esperaba ErrHuecoTomado, se obtuvo %v", err)
	}
}
```

- [ ] **Paso 2: correr el test y verificar que falla**

```bash
go test ./internal/repository/memory/ -run TestTurno -v
```

Esperado: FAIL — `undefined: NuevoTurno` en el paquete `memory`.

- [ ] **Paso 3: escribir la interfaz**

`internal/repository/turno.go`:

```go
package repository

import (
	"context"
	"time"

	"github.com/google/uuid"

	"github.com/joaquinfochoa/Salud/apps/api/internal/domain"
)

// Turno guarda los turnos reservados.
//
// No hay Eliminar: cancelar es un cambio de estado, no un borrado. El turno
// cancelado sigue siendo parte del historial de las dos partes.
//
// Contrato de unicidad, igual que la matrícula: lo garantiza la escritura, no
// el que llama. En PostgreSQL esto son dos constraints de exclusión sobre el
// rango temporal —una por profesional, otra por paciente, ambas filtradas por
// estado='reservado'— y traducir su violación a estos centinelas es todo lo que
// tiene que hacer esa implementación:
//
//   - Crear devuelve ErrHuecoTomado si otro turno activo del mismo profesional
//     pisa el intervalo, ErrPacienteSolapado si el que pisa es del mismo
//     paciente, o ErrIDEnUso.
//   - Actualizar devuelve ErrNoEncontrado.
type Turno interface {
	Crear(ctx context.Context, t domain.Turno) error
	ObtenerPorID(ctx context.Context, id uuid.UUID) (domain.Turno, error)
	Actualizar(ctx context.Context, t domain.Turno) error

	// Los dos listados devuelven los turnos que pisan el intervalo semiabierto
	// [desde, hasta), cancelados incluidos: un turno cancelado es parte del
	// historial y esconderlo sería esconderle al paciente que le cancelaron
	// algo. Quien necesite solo los activos filtra con EstaActivo().
	ListarDeProfesional(ctx context.Context, profesionalID uuid.UUID, desde, hasta time.Time) ([]domain.Turno, error)
	ListarDePaciente(ctx context.Context, pacienteID uuid.UUID, desde, hasta time.Time) ([]domain.Turno, error)
}
```

- [ ] **Paso 4: escribir la implementación en memoria**

`internal/repository/memory/turno.go`. Mismo molde que
`internal/repository/memory/profesional.go:19-70`: aserción de compilación,
`sync.RWMutex`, `conflicto` bajo el lock de escritura, `Clonar()` en ambos
sentidos, y orden total antes de devolver.

```go
// conflicto busca un turno ajeno y activo que pise a t. Solo se llama con el
// lock de escritura tomado.
//
// Un turno cancelado no conflictúa con nada: libera su hueco y libera a su
// paciente. Es la misma regla que aplica CalculoHuecos, escrita en el otro
// lugar donde tiene que valer.
//
// ponytail: scan O(n), igual que profesional.go. Un índice por profesional y
// día lo evita; para un store en memoria de desarrollo no vale el mapa extra.
func (r *Turno) conflicto(t domain.Turno) error {
	if !t.EstaActivo() {
		return nil // un turno que nace cancelado no puede chocar con nadie
	}
	for _, otro := range r.datos {
		if otro.ID == t.ID || !otro.EstaActivo() || !otro.SeSolapaCon(t.Inicio, t.Fin) {
			continue
		}
		if otro.ProfesionalID == t.ProfesionalID {
			return domain.ErrHuecoTomado
		}
		if otro.PacienteID == t.PacienteID {
			return domain.ErrPacienteSolapado
		}
	}
	return nil
}
```

Los dos listados comparten el filtro y el orden:

```go
func (r *Turno) listar(coincide func(domain.Turno) bool, desde, hasta time.Time) []domain.Turno {
	salida := make([]domain.Turno, 0, len(r.datos))
	for _, t := range r.datos {
		if coincide(t) && t.SeSolapaCon(desde, hasta) {
			salida = append(salida, t.Clonar())
		}
	}
	// El mapa de Go itera en orden aleatorio. Sin orden total, dos llamadas
	// idénticas devuelven listas distintas.
	slices.SortFunc(salida, func(a, b domain.Turno) int {
		if c := a.Inicio.Compare(b.Inicio); c != 0 {
			return c
		}
		return strings.Compare(a.ID.String(), b.ID.String())
	})
	return salida
}
```

- [ ] **Paso 5: correr los tests y verificar que pasan**

```bash
go test ./internal/repository/memory/ -run TestTurno -race -v
```

Esperado: PASS, los 10 tests.

- [ ] **Paso 6: commit**

```bash
git add internal/repository/turno.go internal/repository/memory/
git commit -m "feat(repository): repositorio de turnos en memoria"
```

---

## Task 4: servicio de turnos

**Archivos:**
- Crear: `internal/service/turno.go`, `internal/service/turno_test.go`

**Interfaces:**
- Consume: `repository.Turno`, `repository.Profesional`, `*service.Agenda`,
  `verificarPropiedad` (ya existe en `internal/service/profesional.go:53`)
- Produce:
  - `service.NuevoTurno(repo repository.Turno, profesionales repository.Profesional, agenda *Agenda, opciones ...func(*Turno)) *Turno`
  - `service.ConRelojTurno(func() time.Time) func(*Turno)`
  - `Reservar(ctx, pacienteID, profesionalID uuid.UUID, inicio time.Time, motivo string) (domain.Turno, error)`
  - `Cancelar(ctx, usuarioID, turnoID uuid.UUID) error`
  - `ListarDePaciente(ctx, pacienteID uuid.UUID, desde, hasta *time.Time) ([]domain.Turno, error)`
  - `ListarDeProfesional(ctx, usuarioID, profesionalID uuid.UUID, desde, hasta *time.Time) ([]domain.Turno, error)`

- [ ] **Paso 1: escribir el test que falla**

`internal/service/turno_test.go` cubre:

| Test | Verifica |
|---|---|
| `TestReservarUnHueco` | 201: el turno sale con el intervalo y la modalidad del hueco |
| `TestReservarUnInicioQueNoEsHueco` | `ErrorValidacion` nombrando `inicio` |
| `TestReservarFueraDeLaAnticipacionMinima` | mismo error: la regla la aplica `HuecosLibres`, no se reimplementa |
| `TestReservarContraProfesionalInactivo` | mismo error, porque su lista de huecos está vacía |
| `TestReservarDosVecesElMismoHueco` | `ErrHuecoTomado` |
| `TestReservarSolapandoAlPaciente` | dos profesionales distintos, misma hora → `ErrPacienteSolapado` |
| `TestReservarSolapandoUnTurnoCancelado` | si el anterior está cancelado, entra |
| `TestCancelarComoPaciente` / `...ComoProfesional` | los dos pueden |
| `TestCancelarComoTercero` | `ErrTurnoAjeno` |
| `TestListarDePacienteSoloDevuelveLosSuyos` | dos pacientes, cada uno ve el suyo |
| `TestListarDeProfesionalRequiereSerDueno` | otro usuario → `ErrNoAutorizado` |

El más importante es el que prueba que **no se reimplementó ninguna regla**:

```go
// La anticipación mínima no está escrita en ningún lado del servicio de
// turnos: sale gratis porque HuecosLibres ya no genera ese hueco. Si alguien
// "optimiza" Reservar para no consultar los huecos, este test lo atrapa.
func TestReservarFueraDeLaAnticipacionMinima(t *testing.T) {
	// ... crear un profesional con AnticipacionMinimaMin alto y horario hoy,
	// intentar reservar dentro de la ventana
	var verr domain.ErrorValidacion
	if !errors.As(err, &verr) {
		t.Fatalf("se esperaba ErrorValidacion, se obtuvo %v", err)
	}
	if verr.Campos[0].Campo != "inicio" {
		t.Errorf("campo = %q, se esperaba inicio", verr.Campos[0].Campo)
	}
}
```

- [ ] **Paso 2: correr el test y verificar que falla**

```bash
go test ./internal/service/ -run TestReservar -v
```

Esperado: FAIL — `undefined: NuevoTurno` con esa firma.

- [ ] **Paso 3: escribir la implementación**

`internal/service/turno.go`. El corazón es `Reservar`, y es corto a propósito:

```go
// Reservar toma un hueco a nombre del paciente.
//
// La única validación de calendario es que el inicio pedido coincida con un
// hueco libre. Eso arrastra gratis, sin reimplementar nada: el horario
// semanal, los bloqueos, la anticipación mínima, el horizonte, el profesional
// inactivo y los turnos ya tomados. Todo eso ya lo decide HuecosLibres.
//
// La alternativa —validar cada regla otra vez acá— duplicaría seis reglas en
// un segundo lugar que se desincroniza del primero en el primer cambio.
func (s *Turno) Reservar(ctx context.Context, pacienteID, profesionalID uuid.UUID, inicio time.Time, motivo string) (domain.Turno, error) {
	// Se piden los huecos de ese único día. HuecosLibres recorta al horizonte
	// y valida que el profesional exista, así que un profesional inexistente
	// sale por acá con ErrNoEncontrado.
	dia := domain.InicioDelDia(inicio)
	resultado, err := s.agenda.HuecosLibres(ctx, profesionalID, dia, dia)
	if err != nil {
		return domain.Turno{}, err
	}

	i := slices.IndexFunc(resultado.Huecos, func(h domain.Hueco) bool {
		return h.Inicio.Equal(inicio)
	})
	if i < 0 {
		return domain.Turno{}, domain.ErrorValidacion{Campos: []domain.ErrorCampo{
			{Campo: "inicio", Mensaje: "no corresponde a un turno disponible"},
		}}
	}

	turno, err := domain.NuevoTurno(profesionalID, pacienteID, resultado.Huecos[i], motivo, s.ahora())
	if err != nil {
		return domain.Turno{}, err
	}

	// El chequeo de arriba es el camino rápido que da el error lindo; entre
	// leer los huecos y escribir acá entra otro paciente. La garantía la da el
	// repositorio bajo el lock de escritura, con ErrHuecoTomado.
	if err := s.repo.Crear(ctx, turno); err != nil {
		return domain.Turno{}, err
	}
	return turno, nil
}
```

`Cancelar` resuelve los dos dueños:

```go
// Cancelar lo pueden hacer las dos partes. Un turno no tiene un dueño sino
// dos, y por eso no alcanza con verificarPropiedad.
func (s *Turno) Cancelar(ctx context.Context, usuarioID, turnoID uuid.UUID) error {
	turno, err := s.repo.ObtenerPorID(ctx, turnoID)
	if err != nil {
		return err
	}
	if err := s.verificarParte(ctx, turno, usuarioID); err != nil {
		return err
	}

	cancelado, err := turno.Cancelar(usuarioID, s.ahora())
	if err != nil {
		return err
	}
	return s.repo.Actualizar(ctx, cancelado)
}

// verificarParte acepta al paciente del turno y al dueño del perfil
// profesional. Cualquier otro recibe ErrTurnoAjeno.
func (s *Turno) verificarParte(ctx context.Context, t domain.Turno, usuarioID uuid.UUID) error {
	if t.PacienteID == usuarioID {
		return nil
	}
	profesional, err := s.profesionales.ObtenerPorID(ctx, t.ProfesionalID)
	if err != nil {
		return err
	}
	if profesional.UsuarioID == usuarioID {
		return nil
	}
	return domain.ErrTurnoAjeno
}
```

Los dos listados traducen el rango opcional a la misma ventana por defecto que
`ListarBloqueos` —vigentes y futuros— y `ListarDeProfesional` exige ser el
dueño del perfil con el `verificarPropiedad` que ya existe en el paquete.

- [ ] **Paso 4: correr los tests y verificar que pasan**

```bash
go test ./internal/service/ -race -v
```

Esperado: PASS, toda la suite del paquete.

- [ ] **Paso 5: mutation testing de la regla central**

Cambiar el `h.Inicio.Equal(inicio)` de `Reservar` por `true`.

```bash
go test ./internal/service/ -run TestReservarUnInicioQueNoEsHueco
```

Esperado: FAIL. Revertir.

- [ ] **Paso 6: commit**

```bash
git add internal/service/turno.go internal/service/turno_test.go
git commit -m "feat(service): reservar, cancelar y listar turnos"
```

---

## Task 5: contrato OpenAPI

El contrato se escribe **antes** que los handlers, igual que en las tres etapas
anteriores.

**Archivos:**
- Modificar: `api/openapi.yaml`

- [ ] **Paso 1: agregar el tag y los esquemas**

Tag nuevo `turnos`, con las mismas cuatro entradas de `tags:` que ya existen.

En `components/schemas`:

- `EstadoTurno`: `enum: [reservado, cancelado]`.
- `PeticionTurno`: `inicio` (date-time, **required**) y `motivo` (string,
  maxLength 500, opcional). Y nada más — documentar explícitamente en su
  `description` que `fin` y `modalidad` **no** se aceptan del cliente porque
  salen del hueco.
- `Turno`: `id`, `profesionalId`, `pacienteId`, `inicio`, `fin`, `modalidad`,
  `estado`, `motivo`, `creadoEn`, `canceladoEn` (nullable),
  `canceladoPor` (uuid, nullable). Todos `required`, incluidos los dos
  nullables — el handler siempre manda la clave, y dejarlos fuera de `required`
  haría que un cliente estricto los trate como omitibles. Es el minor que quedó
  anotado sobre `dadoDeBajaEn` en la etapa 1; acá se hace bien de entrada.
- `ListaTurnos`: `{ datos: [Turno] }`. **Sin `paginacion`**: no es una colección
  paginada, es una ventana temporal, igual que `ListaBloqueos`.

- [ ] **Paso 2: agregar las cuatro operaciones**

```yaml
  /api/v1/turnos:
    get:      # 200 ListaTurnos — mis turnos; 401
  /api/v1/turnos/{id}:
    delete:   # 204; 401; 403 no sos parte; 404; 422 ya empezó o ya cancelado
  /api/v1/profesionales/{id}/turnos:
    get:      # 200 ListaTurnos — la agenda; 401; 403 no sos el dueño; 404
    post:     # 201 + Location; 401; 409 hueco tomado o paciente solapado;
              # 415; 422 el inicio no es un hueco
```

Las cuatro llevan `security: [{ sesionCookie: [] }]`. **Ninguna es pública**, y
`GET /profesionales/{id}/turnos` en particular tiene que documentar por qué se
aparta de `/horarios` y `/bloqueos`, que sí lo son: los huecos libres son
información de oferta, pero la agenda ocupada dice quién es paciente de quién.

Los dos `GET` aceptan `desde` y `hasta` opcionales, con la misma
`description` que los de `/bloqueos`: sin rango, los vigentes y futuros.

- [ ] **Paso 3: agregar las respuestas nuevas**

En `components/responses`:

```yaml
    TurnoNoDisponible:
      description: |
        El hueco ya fue reservado, o el paciente ya tiene otro turno activo a
        esa hora con otro profesional.
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/Problema' }
```

Y `NoAutorizado`, que ya existe, se reusa para el 403 de "no sos parte de este
turno".

- [ ] **Paso 4: verificar que valida**

```bash
cd ../..    # el redocly.yaml de la raíz baja el ruleset a minimal
npx --yes @redocly/cli@2.47.0 lint apps/api/api/openapi.yaml
```

Esperado: `valid`, cero errores. Los warnings de `operation-operationId`,
`tag-description` y `no-server-example.com` son preexistentes y aceptados.

**Correr desde la raíz del repo, no desde `apps/api/`**: el CLI autodescubre
`redocly.yaml` desde el directorio actual, y desde `apps/api/` no lo encuentra y
aplica el ruleset por defecto, que da diez errores falsos.

- [ ] **Paso 5: commit**

```bash
git add api/openapi.yaml
git commit -m "feat(api): contrato de reserva y cancelacion de turnos"
```

---

## Task 6: endpoints de turnos

**Archivos:**
- Crear: `internal/handler/turno.go`, `internal/handler/dto_turno.go`,
  `internal/handler/turno_test.go`
- Modificar: `internal/handler/router.go`, `internal/handler/problema.go`

**Interfaces:**
- Produce: `handler.NuevoTurno(svc *service.Turno) *ManejadorTurno` con
  `Reservar`, `Cancelar`, `MisTurnos` y `DeProfesional`.
- `NuevoRouter` pasa a recibir un cuarto parámetro: `*ManejadorTurno`.

- [ ] **Paso 1: escribir el test que falla**

`internal/handler/turno_test.go`, contra el servidor real y reusando los helpers
que ya existen (`nuevoStackDePrueba`, `conSesion`, `postear`, `obtener`,
`ejecutar`, definidos en `router_test.go` y `profesional_test.go`):

| Test | Verifica |
|---|---|
| `TestReservarDevuelve201YLocation` | 201 y el header `Location` al turno creado |
| `TestElHuecoDesapareceDespuesDeReservar` | `GET /huecos` antes y después: uno menos |
| `TestCancelarDevuelveElHueco` | y después de cancelar, vuelve a aparecer |
| `TestReservarUnInicioInvalidoDa422` | nombrando `inicio` |
| `TestReservarDosVecesDa409` | |
| `TestReservarSinSesionDa401` | |
| `TestCancelarComoTerceroDa403` | |
| `TestMisTurnosSoloDevuelveLosMios` | dos pacientes en el mismo servidor |
| `TestAgendaDelProfesionalRequiereSerDueno` | 403 para otro usuario |
| `TestReservarConContentTypeInvalidoDa415` | |

- [ ] **Paso 2: correr el test y verificar que falla**

```bash
go test ./internal/handler/ -run TestReservar -v
```

Esperado: FAIL — `undefined: NuevoTurno` con esa firma.

- [ ] **Paso 3: escribir los DTOs**

`internal/handler/dto_turno.go`: `peticionTurno` (solo `inicio` y `motivo`),
`respuestaTurno` y `respuestaListaTurnos`. La clave del listado es `datos`,
igual que `ListaProfesionales` y a diferencia de `ListaHorarios`, que usa
`horarios` — la convención del proyecto es `datos` salvo que haya una razón, y
acá no la hay.

- [ ] **Paso 4: escribir el manejador**

`internal/handler/turno.go`, con los cuatro handlers. Los cuatro empiezan con
`usuarioAutenticado(w, r)` —el helper de `middleware.go:57`— porque los cuatro
son privados.

`Reservar` es el único con cuerpo:

```go
func (h *ManejadorTurno) Reservar(w http.ResponseWriter, r *http.Request) {
	usuarioID, ok := usuarioAutenticado(w, r)
	if !ok {
		return
	}
	profesionalID, ok := parsearID(w, r)
	if !ok {
		return
	}

	var req peticionTurno
	if err := decodificarJSON(w, r, &req); err != nil {
		escribirErrorDeDecodificacion(w, r, err)
		return
	}

	turno, err := h.svc.Reservar(r.Context(), usuarioID, profesionalID, req.Inicio, req.Motivo)
	if err != nil {
		escribirError(w, r, err)
		return
	}

	w.Header().Set("Location", "/api/v1/turnos/"+turno.ID.String())
	escribirJSON(w, http.StatusCreated, aRespuestaTurno(turno))
}
```

- [ ] **Paso 5: mapear los centinelas nuevos**

En `internal/handler/problema.go`, dentro de `escribirError` y **antes del
`default`**:

```go
	case errors.Is(err, domain.ErrHuecoTomado):
		escribirProblema(w, Problema{
			Tipo:    tipoConflicto,
			Titulo:  "El turno ya fue tomado",
			Estado:  http.StatusConflict,
			Detalle: "Alguien reservó ese horario. Elegí otro.",
		})

	case errors.Is(err, domain.ErrPacienteSolapado):
		escribirProblema(w, Problema{
			Tipo:    tipoConflicto,
			Titulo:  "Ya tenés un turno a esa hora",
			Estado:  http.StatusConflict,
			Detalle: "Tenés otro turno que se superpone con este horario",
		})

	case errors.Is(err, domain.ErrTurnoAjeno):
		escribirProblema(w, Problema{
			Tipo:    tipoNoAutorizado,
			Titulo:  "No autorizado",
			Estado:  http.StatusForbidden,
			Detalle: "Solo el paciente o el profesional pueden cancelar este turno",
		})
```

- [ ] **Paso 6: cablear el router**

En `internal/handler/router.go`, cambiar la firma a
`NuevoRouter(ph *ManejadorProfesional, ah *ManejadorAgenda, mh *ManejadorAutenticacion, th *ManejadorTurno)`
y agregar, **las cuatro envueltas en `RequerirSesion`**:

```go
	mux.HandleFunc("GET /api/v1/turnos", RequerirSesion(th.MisTurnos))
	mux.HandleFunc("DELETE /api/v1/turnos/{id}", RequerirSesion(th.Cancelar))
	mux.HandleFunc("GET /api/v1/profesionales/{id}/turnos", RequerirSesion(th.DeProfesional))
	mux.HandleFunc("POST /api/v1/profesionales/{id}/turnos", RequerirSesion(th.Reservar))
```

Sin colisiones: `/api/v1/turnos` es un prefijo nuevo, y
`/profesionales/{id}/turnos` tiene cinco segmentos con el literal `turnos` en la
quinta posición, igual que `horarios`, `bloqueos` y `huecos`, que ya conviven.

Actualizar también `nuevoStackDePrueba` en `router_test.go` y el cableado de
`cmd/api/main.go`.

- [ ] **Paso 7: correr los tests y verificar que pasan**

```bash
go test ./... -race
```

Esperado: PASS en todos los paquetes.

- [ ] **Paso 8: commit**

```bash
git add internal/handler/ cmd/api/main.go
git commit -m "feat(handler): endpoints de reserva y cancelacion de turnos"
```

---

## Task 7: la agenda conoce los turnos

**Es la única tarea que rompe código existente**, y por eso va última. Cambia
dos firmas y una forma de respuesta; el pre-flight contó **20 call sites**.

**Archivos:**
- Modificar: `internal/service/agenda.go`, `internal/service/agenda_test.go`,
  `internal/service/autorizacion_test.go`
- Modificar: `internal/handler/agenda.go`, `internal/handler/dto_agenda.go`,
  `internal/handler/agenda_test.go`, `internal/handler/router_test.go`
- Modificar: `cmd/api/main.go`, `api/openapi.yaml`

**Interfaces:**
- `service.NuevaAgenda(profesionales repository.Profesional, horarios repository.HorarioSemanal, bloqueos repository.Bloqueo, turnos repository.Turno, opciones ...func(*Agenda)) *Agenda`
- `ReemplazarHorarios(...) ([]domain.HorarioSemanal, int, error)` — el `int` son
  los turnos cancelados
- `CrearBloqueo(...) (domain.Bloqueo, int, error)` — idem

- [ ] **Paso 1: escribir los tests que fallan**

```go
// Criterio de aceptación 10.
func TestUnBloqueoCancelaLosTurnosQuePisa(t *testing.T) {
	// ... reservar dos turnos futuros, después bloquear ese rango
	if cancelados != 2 {
		t.Errorf("cancelados = %d, se esperaban 2", cancelados)
	}
	for _, turno := range turnosDespues {
		if turno.EstaActivo() {
			t.Error("quedó un turno activo dentro del bloqueo")
		}
		if turno.CanceladoPor == nil || *turno.CanceladoPor != duenoUsuarioID {
			t.Error("no quedó registrado que canceló el profesional")
		}
	}
}

// Un bloqueo puede cubrir fechas pasadas —"estuve de licencia la semana
// pasada" es un caso real— pero un turno que ya ocurrió no se cancela:
// reescribirlo hacia atrás borraría el registro de una consulta que pasó.
func TestUnBloqueoNoTocaLosTurnosQueYaOcurrieron(t *testing.T) {
	// ... con el reloj inyectado, un turno en el pasado y un bloqueo que lo cubre
	if cancelados != 0 {
		t.Errorf("cancelados = %d, se esperaban 0", cancelados)
	}
	if !turnoPasado.EstaActivo() {
		t.Error("se canceló un turno que ya había ocurrido")
	}
}

func TestSacarUnBloqueHorarioCancelaSusTurnos(t *testing.T) {
	// ... cargar lunes, reservar el lunes, reemplazar la semana sin lunes
}

// Los huecos ya no ofrecen lo reservado. Es el criterio 2 en la capa de
// servicio: sin esto, HuecosLibres tendría el campo Turnos en nil para siempre
// y el dominio nunca vería un turno.
func TestHuecosLibresRestaLosTurnos(t *testing.T) {
	// ... contar huecos, reservar uno, volver a contar
}
```

- [ ] **Paso 2: correr y verificar que no compila**

```bash
go build ./...
```

Esperado: errores por la cantidad de argumentos de `NuevaAgenda` y por los
valores de retorno de las dos operaciones. Es la señal de que las firmas
cambiaron de verdad.

- [ ] **Paso 3: agregar el repositorio a `Agenda`**

`NuevaAgenda` gana `turnos repository.Turno` como cuarto parámetro, antes de las
opciones. En el struct, el campo `turnos`.

> **No recibe `*service.Turno`.** Sería un ciclo: `service.Turno` ya depende de
> `*Agenda` para pedirle los huecos. La regla de cancelar vive en el dominio
> (`Turno.Cancelar`), que los dos importan, así que `Agenda` habla con el
> repositorio y con el dominio, nunca con el otro servicio.

- [ ] **Paso 4: restar los turnos en `HuecosLibres`**

Cargar los turnos del rango junto a los bloqueos y pasarlos al cálculo:

```go
	turnos, err := s.turnos.ListarDeProfesional(ctx, profesionalID, desde, finExclusivo)
	if err != nil {
		return ResultadoHuecos{}, err
	}
```

y agregar `Turnos: turnos` al literal de `domain.CalculoHuecos`. Se pasan
todos, cancelados incluidos: filtrar es trabajo del dominio, que ya lo hace con
`EstaActivo()`, y hacerlo dos veces es la forma de que un día no coincidan.

- [ ] **Paso 5: cancelar los turnos que el bloqueo pisa**

Un helper privado, usado por las dos operaciones:

```go
// cancelarTurnosEn cancela los turnos activos y futuros del profesional que
// pisan [desde, hasta), y devuelve cuántos canceló.
//
// Solo los futuros: un turno que ya empezó no se cancela, y domain.Cancelar
// lo rechaza. Un bloqueo sobre fechas pasadas es legítimo —cargar una licencia
// vieja— y no tiene por qué reescribir la historia.
func (s *Agenda) cancelarTurnosEn(ctx context.Context, profesionalID, porUsuarioID uuid.UUID, desde, hasta time.Time) (int, error) {
	turnos, err := s.turnos.ListarDeProfesional(ctx, profesionalID, desde, hasta)
	if err != nil {
		return 0, err
	}

	ahora := s.ahora()
	cancelados := 0
	for _, t := range turnos {
		if !t.EstaActivo() || !t.Inicio.After(ahora) {
			continue
		}
		cancelado, err := t.Cancelar(porUsuarioID, ahora)
		if err != nil {
			return 0, err
		}
		if err := s.turnos.Actualizar(ctx, cancelado); err != nil {
			return 0, err
		}
		cancelados++
	}
	return cancelados, nil
}
```

`CrearBloqueo` lo llama con el rango del bloqueo, después de escribirlo.
`ReemplazarHorarios` lo llama por cada día del horizonte que **antes** tenía un
bloque y ahora no —hay que comparar la semana vieja contra la nueva—, o más
simple y más correcto: **recalcular** y cancelar todo turno futuro que ya no
caiga en un hueco de la semana nueva.

> **ponytail:** recalcular recorre el horizonte entero (hasta 180 días) en cada
> `PUT /horarios`. Es O(horizonte × bloques) sobre datos en memoria y esta
> operación se llama una vez cada muchos meses. Comparar semana vieja contra
> nueva sería más rápido y bastante más fácil de escribir mal.

- [ ] **Paso 6: propagar el contador hasta la respuesta**

Las dos respuestas del handler ganan `turnosCancelados` (integer, siempre
presente, cero cuando no canceló nada). Actualizar `dto_agenda.go`,
`handler/agenda.go` y los dos esquemas de `api/openapi.yaml`.

Es un cambio de contrato en dos operaciones existentes y hay que decirlo en su
`description`: el profesional se entera **en el momento** de cuántos turnos
canceló, no el día del turno.

- [ ] **Paso 7: arreglar los 20 call sites**

```bash
go vet ./... 2>&1 | head -30
```

Seis de `NuevaAgenda` —`cmd/api/main.go`, `router_test.go`, `agenda_test.go`
(dos) y `autorizacion_test.go` (dos)— y catorce de las dos operaciones en los
tests. A los tests que no verifican cancelaciones se les descarta el contador
con `_`.

- [ ] **Paso 8: correr todo y validar el contrato**

```bash
go test ./... -race
cd ../.. && npx --yes @redocly/cli@2.47.0 lint apps/api/api/openapi.yaml
```

Esperado: PASS y `valid`.

- [ ] **Paso 9: commit**

```bash
make check
git add -A
git commit -m "feat: la agenda conoce los turnos"
```

---

## Task 8: verificación final

Sin commit propio salvo que aparezca algo para arreglar.

- [ ] **Paso 1: la suite completa**

```bash
make check
```

Esperado: verde. **Criterio 1.**

- [ ] **Paso 2: recorrer el circuito completo contra el servidor real**

```bash
make run
```

En otra terminal, con `curl -c/-b` para llevar la cookie:

| # | Qué | Esperado | Criterio |
|---|---|---|---|
| 1 | Login del seed, cargar horarios del lunes | 200 | |
| 2 | `GET /huecos` y contar | N huecos | |
| 3 | Registrar un paciente distinto y reservar el primer hueco | 201 + `Location` | |
| 4 | `GET /huecos` otra vez | **N−1**, y el reservado no está | **2** |
| 5 | Reservar un `inicio` de las 03:00 | **422** nombrando `inicio` | **3** |
| 6 | Reservar el mismo hueco desde otra cuenta | **409** | **4** |
| 7 | Con un tercer profesional, reservar la misma hora que el paso 3 | **409** | **5** |
| 8 | Cancelar como un tercero | **403** | **6** |
| 9 | Cancelar como el paciente | 204 | **6** |
| 10 | `GET /huecos` | vuelve a ser **N** | **7** |
| 11 | `GET /turnos` con y sin cookie | 200 solo los propios / **401** | **8** |
| 12 | `GET /profesionales/{id}/turnos` como otro usuario | **403** | **9** |
| 13 | Reservar dos, bloquear ese rango, mirar la respuesta | `turnosCancelados: 2` | **10** |
| 14 | Dar de baja al profesional y pedir huecos | lista vacía; reservar da 422 | **11** |

- [ ] **Paso 3: mutation testing de las tres reglas que sostienen la etapa**

Los tests que verifican una prohibición pasan igual si la prohibición no existe.
Romperla a propósito es la única forma de saberlo.

1. `Reservar`: cambiar `h.Inicio.Equal(inicio)` por `true`.
   → tiene que fallar `TestReservarUnInicioQueNoEsHueco`.
2. `CalculoHuecos.ocupado`: sacar el `turno.EstaActivo() &&`.
   → tiene que fallar `TestHuecosNoRestanTurnosCancelados`.
3. `cancelarTurnosEn`: sacar el `!t.Inicio.After(ahora)`.
   → tiene que fallar `TestUnBloqueoNoTocaLosTurnosQueYaOcurrieron`.

Revertir las tres.

- [ ] **Paso 4: verificar el aislamiento del dominio**

```bash
go list -deps ./internal/domain/ | grep 'joaquinfochoa' | grep -v 'internal/domain'
```

Esperado: sin salida.

- [ ] **Paso 5: cruzar el router contra el contrato**

Cada ruta de `router.go` tiene que estar en `openapi.yaml` con el mismo método,
y **cada operación envuelta en `RequerirSesion` tiene que declarar
`security: sesionCookie`**, y viceversa. Las dos listas tienen que coincidir
exactamente: una ruta protegida en el contrato pero abierta en el código es un
agujero que ningún test atrapa.

Con las cuatro rutas nuevas, las privadas pasan de 8 a **12**.

- [ ] **Paso 6: registrar el progreso**

Agregar la sección "Etapa 4 — Turno" a `.superpowers/sdd/progress.md`, con el
estado de cada tarea y los minor pendientes, igual que las tres etapas
anteriores.

---

## Notas de riesgo

**La Task 7 es la única que rompe, y es la última.** Eso es deliberado y viene
del pre-flight: en la etapa de autenticación la ruptura estaba en la tarea 7 de
15 y cascadeó hacia adelante y hacia atrás, obligando a fusionar cinco tareas en
un commit. Acá todo lo aditivo entra limpio antes.

**El riesgo silencioso está en la Task 4.** Si alguien "optimiza" `Reservar`
para no consultar `HuecosLibres` y validar las reglas a mano, todo sigue
compilando y la mayoría de los tests siguen pasando —el camino feliz funciona
igual—, pero la anticipación mínima, el horizonte y el estado del profesional
dejan de aplicarse en el alta. Los tests
`TestReservarFueraDeLaAnticipacionMinima` y `TestReservarContraProfesionalInactivo`
existen exactamente para eso: no prueban esas reglas, prueban que **no se
reimplementaron**.

**Lo que esta etapa deja sin cerrar, a propósito:** el paciente al que le
cancelan un turno se entera entrando a la app. Sin notificaciones, cancelar
automáticamente sigue siendo mejor que dejar turnos fantasma, pero no es
aceptable con usuarios reales. Va a la misma lista que el rate limiting del
login: **antes de exponer la API**.
